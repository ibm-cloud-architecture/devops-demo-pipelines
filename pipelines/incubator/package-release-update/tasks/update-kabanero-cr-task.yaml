apiVersion: tekton.dev/v1alpha1
kind: Task
metadata:
  name: update-kabanero-cr-task
  namespace: kabanero
  author: Oscar I. Ricaud
  date: 04/21/20
spec:
  inputs:
    params:
      - default: googlefu/apic_compiler:v3-amd64
        name: asuh-image
        type: string
    resources:
      - name: source
        type: git
      - name: image
        type: image
  steps:
    - args:
        - -c
        - |

          echo "delete this task"

      command:
        - /bin/bash
      resources: {}
      name: update-kabanero-cr-task
      image: $(inputs.params.asuh-image)
      workingDir: $(inputs.resources.source.path)

